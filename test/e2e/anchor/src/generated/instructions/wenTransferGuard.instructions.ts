/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    assertIsInstructionWithAccounts,
    containsBytes,
    type Instruction,
    type InstructionWithData,
    type ReadonlyUint8Array,
} from '@solana/kit';
import {
    CREATE_GUARD_DISCRIMINATOR,
    parseCreateGuardInstruction,
    type ParsedCreateGuardInstruction,
} from './createGuard.js';
import { EXECUTE_DISCRIMINATOR, parseExecuteInstruction, type ParsedExecuteInstruction } from './execute.js';
import {
    INITIALIZE_DISCRIMINATOR,
    parseInitializeInstruction,
    type ParsedInitializeInstruction,
} from './initialize.js';
import {
    parseUpdateGuardInstruction,
    type ParsedUpdateGuardInstruction,
    UPDATE_GUARD_DISCRIMINATOR,
} from './updateGuard.js';

/** Instruction kinds of the wenTransferGuard program. */
export type WenTransferGuardInstructionType = 'createGuard' | 'execute' | 'initialize' | 'updateGuard';

/**
 * Identifies wenTransferGuard instruction data by its discriminators.
 * Returns `null` when the data matches no known instruction.
 */
export function identifyWenTransferGuardInstruction(
    instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): WenTransferGuardInstructionType | null {
    const data = 'data' in instruction ? instruction.data : instruction;
    if (containsBytes(data, CREATE_GUARD_DISCRIMINATOR, 0)) {
        return 'createGuard';
    }
    if (containsBytes(data, EXECUTE_DISCRIMINATOR, 0)) {
        return 'execute';
    }
    if (containsBytes(data, INITIALIZE_DISCRIMINATOR, 0)) {
        return 'initialize';
    }
    if (containsBytes(data, UPDATE_GUARD_DISCRIMINATOR, 0)) {
        return 'updateGuard';
    }
    return null;
}

/** Parsed wenTransferGuard instruction: the instruction kind tag plus its parsed accounts and data. */
export type ParsedWenTransferGuardInstruction<TProgram extends string = 'LockdqYQ9X2kwtWB99ioSbxubAmEi8o9jqYwbXgrrRw'> =
    | ({ instructionType: 'createGuard' } & ParsedCreateGuardInstruction<TProgram>)
    | ({ instructionType: 'execute' } & ParsedExecuteInstruction<TProgram>)
    | ({ instructionType: 'initialize' } & ParsedInitializeInstruction<TProgram>)
    | ({ instructionType: 'updateGuard' } & ParsedUpdateGuardInstruction<TProgram>);

/**
 * Parses a wenTransferGuard instruction into its kind tag plus parsed accounts and data.
 * Returns `null` when no known instruction matches; throws if a matched instruction fails to parse.
 */
export function parseWenTransferGuardInstruction<TProgram extends string>(
    instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedWenTransferGuardInstruction<TProgram> | null {
    const instructionType = identifyWenTransferGuardInstruction(instruction);
    if (instructionType === null) return null;
    switch (instructionType) {
        case 'createGuard': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'createGuard', ...parseCreateGuardInstruction(instruction) };
        }
        case 'execute': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'execute', ...parseExecuteInstruction(instruction) };
        }
        case 'initialize': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'initialize', ...parseInitializeInstruction(instruction) };
        }
        case 'updateGuard': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'updateGuard', ...parseUpdateGuardInstruction(instruction) };
        }
    }
}
