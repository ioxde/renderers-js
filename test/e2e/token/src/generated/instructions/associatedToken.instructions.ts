/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    assertIsInstructionWithAccounts,
    containsBytes,
    getU8Encoder,
    type Instruction,
    type InstructionWithData,
    type ReadonlyUint8Array,
} from '@solana/kit';
import {
    CREATE_ASSOCIATED_TOKEN_DISCRIMINATOR,
    parseCreateAssociatedTokenInstruction,
    type ParsedCreateAssociatedTokenInstruction,
} from './createAssociatedToken.js';
import {
    CREATE_ASSOCIATED_TOKEN_IDEMPOTENT_DISCRIMINATOR,
    parseCreateAssociatedTokenIdempotentInstruction,
    type ParsedCreateAssociatedTokenIdempotentInstruction,
} from './createAssociatedTokenIdempotent.js';
import {
    parseRecoverNestedAssociatedTokenInstruction,
    RECOVER_NESTED_ASSOCIATED_TOKEN_DISCRIMINATOR,
    type ParsedRecoverNestedAssociatedTokenInstruction,
} from './recoverNestedAssociatedToken.js';

/** Instruction kinds of the associatedToken program. */
export type AssociatedTokenInstructionType =
    | 'createAssociatedToken'
    | 'createAssociatedTokenIdempotent'
    | 'recoverNestedAssociatedToken';

/**
 * Identifies associatedToken instruction data by its discriminators.
 * Returns `null` when the data matches no known instruction.
 */
export function identifyAssociatedTokenInstruction(
    instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): AssociatedTokenInstructionType | null {
    const data = 'data' in instruction ? instruction.data : instruction;
    if (containsBytes(data, getU8Encoder().encode(CREATE_ASSOCIATED_TOKEN_DISCRIMINATOR), 0)) {
        return 'createAssociatedToken';
    }
    if (containsBytes(data, getU8Encoder().encode(CREATE_ASSOCIATED_TOKEN_IDEMPOTENT_DISCRIMINATOR), 0)) {
        return 'createAssociatedTokenIdempotent';
    }
    if (containsBytes(data, getU8Encoder().encode(RECOVER_NESTED_ASSOCIATED_TOKEN_DISCRIMINATOR), 0)) {
        return 'recoverNestedAssociatedToken';
    }
    return null;
}

/** Parsed associatedToken instruction: the instruction kind tag plus its parsed accounts and data. */
export type ParsedAssociatedTokenInstruction<TProgram extends string = 'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'> =
    | ({ instructionType: 'createAssociatedToken' } & ParsedCreateAssociatedTokenInstruction<TProgram>)
    | ({
          instructionType: 'createAssociatedTokenIdempotent';
      } & ParsedCreateAssociatedTokenIdempotentInstruction<TProgram>)
    | ({ instructionType: 'recoverNestedAssociatedToken' } & ParsedRecoverNestedAssociatedTokenInstruction<TProgram>);

/**
 * Parses a associatedToken instruction into its kind tag plus parsed accounts and data.
 * Returns `null` when no known instruction matches; throws if a matched instruction fails to parse.
 */
export function parseAssociatedTokenInstruction<TProgram extends string>(
    instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedAssociatedTokenInstruction<TProgram> | null {
    const instructionType = identifyAssociatedTokenInstruction(instruction);
    if (instructionType === null) return null;
    switch (instructionType) {
        case 'createAssociatedToken': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'createAssociatedToken', ...parseCreateAssociatedTokenInstruction(instruction) };
        }
        case 'createAssociatedTokenIdempotent': {
            assertIsInstructionWithAccounts(instruction);
            return {
                instructionType: 'createAssociatedTokenIdempotent',
                ...parseCreateAssociatedTokenIdempotentInstruction(instruction),
            };
        }
        case 'recoverNestedAssociatedToken': {
            assertIsInstructionWithAccounts(instruction);
            return {
                instructionType: 'recoverNestedAssociatedToken',
                ...parseRecoverNestedAssociatedTokenInstruction(instruction),
            };
        }
    }
}
