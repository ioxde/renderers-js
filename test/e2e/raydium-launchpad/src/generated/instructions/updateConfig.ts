/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    assertIsInstructionForProgram,
    combineCodec,
    containsBytes,
    fixDecoderSize,
    fixEncoderSize,
    getBytesDecoder,
    getBytesEncoder,
    getStructDecoder,
    getStructEncoder,
    getU64Decoder,
    getU64Encoder,
    getU8Decoder,
    getU8Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlySignerAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/kit/program-client-core';
import { RAYDIUM_LAUNCHPAD_PROGRAM_ADDRESS } from '../programs/index.js';

export const UPDATE_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([29, 158, 252, 191, 10, 83, 219, 99]);

export function getUpdateConfigDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_CONFIG_DISCRIMINATOR);
}

export type UpdateConfigInstruction<
    TProgram extends string = typeof RAYDIUM_LAUNCHPAD_PROGRAM_ADDRESS,
    TAccountOwner extends string | AccountMeta<string> = 'GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ',
    TAccountGlobalConfig extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountOwner extends string
                ? ReadonlySignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
                : TAccountOwner,
            TAccountGlobalConfig extends string ? WritableAccount<TAccountGlobalConfig> : TAccountGlobalConfig,
            ...TRemainingAccounts,
        ]
    >;

export type UpdateConfigInstructionData = { discriminator: ReadonlyUint8Array; param: number; value: bigint };

export type UpdateConfigInstructionDataArgs = { param: number; value: number | bigint };

export function getUpdateConfigInstructionDataEncoder(): FixedSizeEncoder<UpdateConfigInstructionDataArgs> {
    return transformEncoder(
        getStructEncoder([
            ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
            ['param', getU8Encoder()],
            ['value', getU64Encoder()],
        ]),
        value => ({ ...value, discriminator: UPDATE_CONFIG_DISCRIMINATOR }),
    );
}

export function getUpdateConfigInstructionDataDecoder(): FixedSizeDecoder<UpdateConfigInstructionData> {
    return getStructDecoder([
        ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
        ['param', getU8Decoder()],
        ['value', getU64Decoder()],
    ]);
}

export function getUpdateConfigInstructionDataCodec(): FixedSizeCodec<
    UpdateConfigInstructionDataArgs,
    UpdateConfigInstructionData
> {
    return combineCodec(getUpdateConfigInstructionDataEncoder(), getUpdateConfigInstructionDataDecoder());
}

export type UpdateConfigInput<TAccountOwner extends string = string, TAccountGlobalConfig extends string = string> = {
    /** The global config owner or admin */
    owner?: TransactionSigner<TAccountOwner>;
    /** Global config account to be changed */
    globalConfig: Address<TAccountGlobalConfig>;
    param: UpdateConfigInstructionDataArgs['param'];
    value: UpdateConfigInstructionDataArgs['value'];
};

export function getUpdateConfigInstruction<
    TAccountOwner extends string,
    TAccountGlobalConfig extends string,
    TProgramAddress extends Address = typeof RAYDIUM_LAUNCHPAD_PROGRAM_ADDRESS,
>(
    input: UpdateConfigInput<TAccountOwner, TAccountGlobalConfig>,
    config?: { programAddress?: TProgramAddress },
): UpdateConfigInstruction<TProgramAddress, TAccountOwner, TAccountGlobalConfig> {
    // Program address.
    const programAddress = config?.programAddress ?? RAYDIUM_LAUNCHPAD_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        owner: { value: input.owner ?? null, isWritable: false },
        globalConfig: { value: input.globalConfig ?? null, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    // Resolve default values.
    if (!accounts.owner.value) {
        accounts.owner.value =
            'GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ' as Address<'GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ'>;
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [getAccountMeta('owner', accounts.owner), getAccountMeta('globalConfig', accounts.globalConfig)],
        data: getUpdateConfigInstructionDataEncoder().encode(args as UpdateConfigInstructionDataArgs),
        programAddress,
    } as UpdateConfigInstruction<TProgramAddress, TAccountOwner, TAccountGlobalConfig>);
}

export type ParsedUpdateConfigInstruction<
    TProgram extends string = typeof RAYDIUM_LAUNCHPAD_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        /** The global config owner or admin */
        owner: TAccountMetas[0];
        /** Global config account to be changed */
        globalConfig: TAccountMetas[1];
    };
    data: UpdateConfigInstructionData;
};

export function parseUpdateConfigInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateConfigInstruction<TProgram, TAccountMetas> {
    assertIsInstructionForProgram(instruction, RAYDIUM_LAUNCHPAD_PROGRAM_ADDRESS);
    if (!containsBytes(instruction.data, UPDATE_CONFIG_DISCRIMINATOR, 0)) {
        const error = new Error(
            `parseUpdateConfigInstruction: instruction data does not match the UpdateConfig discriminator`,
        );
        error.name = 'InstructionDiscriminatorMismatchError';
        throw error;
    }
    if (instruction.accounts.length < 2) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 2,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { owner: getNextAccount(), globalConfig: getNextAccount() },
        data: getUpdateConfigInstructionDataDecoder().decode(instruction.data),
    };
}
