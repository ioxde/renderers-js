/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    assertIsInstructionWithAccounts,
    containsBytes,
    type Address,
    type Instruction,
    type InstructionWithData,
    type ReadonlyUint8Array,
} from '@solana/kit';
import { RAYDIUM_CP_SWAP_PROGRAM_ADDRESS } from '../programs/index.js';
import {
    COLLECT_FUND_FEE_DISCRIMINATOR,
    parseCollectFundFeeInstruction,
    type ParsedCollectFundFeeInstruction,
} from './collectFundFee.js';
import {
    COLLECT_PROTOCOL_FEE_DISCRIMINATOR,
    parseCollectProtocolFeeInstruction,
    type ParsedCollectProtocolFeeInstruction,
} from './collectProtocolFee.js';
import {
    CREATE_AMM_CONFIG_DISCRIMINATOR,
    parseCreateAmmConfigInstruction,
    type ParsedCreateAmmConfigInstruction,
} from './createAmmConfig.js';
import { DEPOSIT_DISCRIMINATOR, parseDepositInstruction, type ParsedDepositInstruction } from './deposit.js';
import {
    INITIALIZE_DISCRIMINATOR,
    parseInitializeInstruction,
    type ParsedInitializeInstruction,
} from './initialize.js';
import {
    parseSwapBaseInputInstruction,
    SWAP_BASE_INPUT_DISCRIMINATOR,
    type ParsedSwapBaseInputInstruction,
} from './swapBaseInput.js';
import {
    parseSwapBaseOutputInstruction,
    SWAP_BASE_OUTPUT_DISCRIMINATOR,
    type ParsedSwapBaseOutputInstruction,
} from './swapBaseOutput.js';
import {
    parseUpdateAmmConfigInstruction,
    type ParsedUpdateAmmConfigInstruction,
    UPDATE_AMM_CONFIG_DISCRIMINATOR,
} from './updateAmmConfig.js';
import {
    parseUpdatePoolStatusInstruction,
    type ParsedUpdatePoolStatusInstruction,
    UPDATE_POOL_STATUS_DISCRIMINATOR,
} from './updatePoolStatus.js';
import { parseWithdrawInstruction, type ParsedWithdrawInstruction, WITHDRAW_DISCRIMINATOR } from './withdraw.js';

/** Instruction kinds of the raydiumCpSwap program. */
export type RaydiumCpSwapInstructionType =
    | 'collectFundFee'
    | 'collectProtocolFee'
    | 'createAmmConfig'
    | 'deposit'
    | 'initialize'
    | 'swapBaseInput'
    | 'swapBaseOutput'
    | 'updateAmmConfig'
    | 'updatePoolStatus'
    | 'withdraw';

/**
 * Identifies raydiumCpSwap instruction data by its discriminators.
 * Returns `null` when the instruction is for another program or the data matches no known instruction.
 */
export function identifyRaydiumCpSwapInstruction(
    instruction: { data: ReadonlyUint8Array; programAddress: Address } | ReadonlyUint8Array,
): RaydiumCpSwapInstructionType | null {
    if ('data' in instruction && instruction.programAddress !== RAYDIUM_CP_SWAP_PROGRAM_ADDRESS) return null;
    const data = 'data' in instruction ? instruction.data : instruction;
    if (containsBytes(data, COLLECT_FUND_FEE_DISCRIMINATOR, 0)) {
        return 'collectFundFee';
    }
    if (containsBytes(data, COLLECT_PROTOCOL_FEE_DISCRIMINATOR, 0)) {
        return 'collectProtocolFee';
    }
    if (containsBytes(data, CREATE_AMM_CONFIG_DISCRIMINATOR, 0)) {
        return 'createAmmConfig';
    }
    if (containsBytes(data, DEPOSIT_DISCRIMINATOR, 0)) {
        return 'deposit';
    }
    if (containsBytes(data, INITIALIZE_DISCRIMINATOR, 0)) {
        return 'initialize';
    }
    if (containsBytes(data, SWAP_BASE_INPUT_DISCRIMINATOR, 0)) {
        return 'swapBaseInput';
    }
    if (containsBytes(data, SWAP_BASE_OUTPUT_DISCRIMINATOR, 0)) {
        return 'swapBaseOutput';
    }
    if (containsBytes(data, UPDATE_AMM_CONFIG_DISCRIMINATOR, 0)) {
        return 'updateAmmConfig';
    }
    if (containsBytes(data, UPDATE_POOL_STATUS_DISCRIMINATOR, 0)) {
        return 'updatePoolStatus';
    }
    if (containsBytes(data, WITHDRAW_DISCRIMINATOR, 0)) {
        return 'withdraw';
    }
    return null;
}

/** Parsed raydiumCpSwap instruction: the instruction kind tag plus its parsed accounts and data. */
export type ParsedRaydiumCpSwapInstruction<TProgram extends string = 'CPMMoo8L3F4NbTegBCKVNunggL7H1ZpdTHKxQB5qKP1C'> =
    | ({ instructionType: 'collectFundFee' } & ParsedCollectFundFeeInstruction<TProgram>)
    | ({ instructionType: 'collectProtocolFee' } & ParsedCollectProtocolFeeInstruction<TProgram>)
    | ({ instructionType: 'createAmmConfig' } & ParsedCreateAmmConfigInstruction<TProgram>)
    | ({ instructionType: 'deposit' } & ParsedDepositInstruction<TProgram>)
    | ({ instructionType: 'initialize' } & ParsedInitializeInstruction<TProgram>)
    | ({ instructionType: 'swapBaseInput' } & ParsedSwapBaseInputInstruction<TProgram>)
    | ({ instructionType: 'swapBaseOutput' } & ParsedSwapBaseOutputInstruction<TProgram>)
    | ({ instructionType: 'updateAmmConfig' } & ParsedUpdateAmmConfigInstruction<TProgram>)
    | ({ instructionType: 'updatePoolStatus' } & ParsedUpdatePoolStatusInstruction<TProgram>)
    | ({ instructionType: 'withdraw' } & ParsedWithdrawInstruction<TProgram>);

/**
 * Parses a raydiumCpSwap instruction into its kind tag plus parsed accounts and data.
 * Returns `null` when the instruction is for another program or no known instruction matches;
 * throws if a matched instruction fails to parse.
 */
export function parseRaydiumCpSwapInstruction<TProgram extends string>(
    instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedRaydiumCpSwapInstruction<TProgram> | null {
    if (instruction.programAddress !== RAYDIUM_CP_SWAP_PROGRAM_ADDRESS) return null;
    const instructionType = identifyRaydiumCpSwapInstruction(instruction);
    if (instructionType === null) return null;
    switch (instructionType) {
        case 'collectFundFee': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'collectFundFee', ...parseCollectFundFeeInstruction(instruction) };
        }
        case 'collectProtocolFee': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'collectProtocolFee', ...parseCollectProtocolFeeInstruction(instruction) };
        }
        case 'createAmmConfig': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'createAmmConfig', ...parseCreateAmmConfigInstruction(instruction) };
        }
        case 'deposit': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'deposit', ...parseDepositInstruction(instruction) };
        }
        case 'initialize': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'initialize', ...parseInitializeInstruction(instruction) };
        }
        case 'swapBaseInput': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'swapBaseInput', ...parseSwapBaseInputInstruction(instruction) };
        }
        case 'swapBaseOutput': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'swapBaseOutput', ...parseSwapBaseOutputInstruction(instruction) };
        }
        case 'updateAmmConfig': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'updateAmmConfig', ...parseUpdateAmmConfigInstruction(instruction) };
        }
        case 'updatePoolStatus': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'updatePoolStatus', ...parseUpdatePoolStatusInstruction(instruction) };
        }
        case 'withdraw': {
            assertIsInstructionWithAccounts(instruction);
            return { instructionType: 'withdraw', ...parseWithdrawInstruction(instruction) };
        }
    }
}
