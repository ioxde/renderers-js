/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { containsBytes, type ReadonlyUint8Array } from '@solana/kit';
import { getLpChangeEventDecoder, LP_CHANGE_EVENT_DISCRIMINATOR, type LpChangeEvent } from './lpChangeEvent.js';
import { getSwapEventDecoder, SWAP_EVENT_DISCRIMINATOR, type SwapEvent } from './swapEvent.js';

/** Event kinds of the raydiumCpSwap program. */
export type RaydiumCpSwapEventType = 'lpChangeEvent' | 'swapEvent';

/**
 * Identifies raydiumCpSwap event data by its discriminators.
 * Returns `null` when the data matches no known event.
 */
export function identifyRaydiumCpSwapEvent(
    event: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): RaydiumCpSwapEventType | null {
    const data = 'data' in event ? event.data : event;
    if (containsBytes(data, LP_CHANGE_EVENT_DISCRIMINATOR, 0)) {
        return 'lpChangeEvent';
    }
    if (containsBytes(data, SWAP_EVENT_DISCRIMINATOR, 0)) {
        return 'swapEvent';
    }
    return null;
}

/** Parsed raydiumCpSwap event: the event kind tag plus its decoded payload. */
export type ParsedRaydiumCpSwapEvent =
    | { eventType: 'lpChangeEvent'; data: LpChangeEvent }
    | { eventType: 'swapEvent'; data: SwapEvent };

/**
 * Parses raydiumCpSwap event data into its event kind and decoded payload.
 * Returns `null` when no known event matches; throws if a matched event fails to decode.
 */
export function parseRaydiumCpSwapEvent(
    event: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): ParsedRaydiumCpSwapEvent | null {
    const data = 'data' in event ? event.data : event;
    const eventType = identifyRaydiumCpSwapEvent(event);
    if (eventType === null) return null;
    switch (eventType) {
        case 'lpChangeEvent': {
            return {
                eventType: 'lpChangeEvent',
                data: getLpChangeEventDecoder().decode(data, LP_CHANGE_EVENT_DISCRIMINATOR.length),
            };
        }
        case 'swapEvent': {
            return {
                eventType: 'swapEvent',
                data: getSwapEventDecoder().decode(data, SWAP_EVENT_DISCRIMINATOR.length),
            };
        }
    }
}
