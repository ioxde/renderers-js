/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    containsBytes,
    fixEncoderSize,
    getAddressDecoder,
    getBytesEncoder,
    getStructDecoder,
    type Address,
    type FixedSizeDecoder,
    type ReadonlyUint8Array,
} from '@solana/kit';
import { ALPHA_PROGRAM_ADDRESS } from '../programs/index.js';
import { ANCHOR_EVENT_CPI_DISCRIMINATOR } from './anchorEventCpiDiscriminator.framing.js';

export const ALPHA_TRADE_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
    189, 219, 127, 211, 78, 230, 97, 238,
]);

export function getAlphaTradeEventDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(ALPHA_TRADE_EVENT_DISCRIMINATOR);
}

/** Discriminator bddb7fd34ee661ee is sha256("event:TradeEvent")[..8], the bytes Anchor derives for any event named TradeEvent. Its body is a strict prefix of the beta event's, so beta's bytes decode here without error. */
export type AlphaTradeEvent = { poolState: Address };

let getAlphaTradeEventDecoderCache: FixedSizeDecoder<AlphaTradeEvent> | undefined;

export function getAlphaTradeEventDecoder(): FixedSizeDecoder<AlphaTradeEvent> {
    return (getAlphaTradeEventDecoderCache ??= getStructDecoder([['poolState', getAddressDecoder()]]));
}

/**
 * Checks whether the event data matches the framing and discriminator bytes of a
 * {@link AlphaTradeEvent}, without decoding. Never throws.
 *
 * Returns `false` unless `event.programAddress` is ALPHA_PROGRAM_ADDRESS.
 * Raw bytes carry no emitter and SKIP that check.
 *
 * @see parseAlphaTradeEvent to decode the matching data
 */
export function isAlphaTradeEvent(
    event: { data: ReadonlyUint8Array; programAddress: Address } | ReadonlyUint8Array,
): boolean {
    if ('data' in event && event.programAddress !== ALPHA_PROGRAM_ADDRESS) return false;
    const data = 'data' in event ? event.data : event;
    return (
        containsBytes(data, ANCHOR_EVENT_CPI_DISCRIMINATOR, 0) &&
        containsBytes(data, ALPHA_TRADE_EVENT_DISCRIMINATOR, 8)
    );
}

/**
 * Parses raw event data as a {@link AlphaTradeEvent}. Returns `null` on framing or discriminator
 * mismatch; throws if the event matches but its body fails to decode.
 *
 * Returns `null` unless `event.programAddress` is ALPHA_PROGRAM_ADDRESS.
 * Raw bytes carry no emitter and SKIP that check.
 *
 * @see isAlphaTradeEvent to check without decoding
 * @see identifyAlphaEvent to identify any program event
 * @see parseAlphaEvent
 */
export function parseAlphaTradeEvent(
    event: { data: ReadonlyUint8Array; programAddress: Address } | ReadonlyUint8Array,
): AlphaTradeEvent | null {
    const checkedEvent = 'data' in event ? { data: event.data, programAddress: event.programAddress } : event;
    if (!isAlphaTradeEvent(checkedEvent)) {
        return null;
    }
    const data = 'data' in checkedEvent ? checkedEvent.data : checkedEvent;
    return getAlphaTradeEventDecoder().decode(
        data,
        ANCHOR_EVENT_CPI_DISCRIMINATOR.length + ALPHA_TRADE_EVENT_DISCRIMINATOR.length,
    );
}
