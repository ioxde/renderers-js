/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    containsBytes,
    fixEncoderSize,
    getAddressDecoder,
    getBytesEncoder,
    getStructDecoder,
    getU64Decoder,
    type Address,
    type FixedSizeDecoder,
    type ReadonlyUint8Array,
} from '@solana/kit';
import { BETA_PROGRAM_ADDRESS } from '../programs/index.js';
import { ANCHOR_EVENT_CPI_DISCRIMINATOR } from './anchorEventCpiDiscriminator.framing.js';

export const BETA_TRADE_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
    189, 219, 127, 211, 78, 230, 97, 238,
]);

export function getBetaTradeEventDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(BETA_TRADE_EVENT_DISCRIMINATOR);
}

/** Its discriminator is deliberately set to alpha's bytes rather than its own sha256("event:BetaTradeEvent")[..8] = 2a77096631cf87ed, modelling two programs that each declare an event named TradeEvent. The two events are then separable only by the emitting program. */
export type BetaTradeEvent = { poolState: Address; amountIn: bigint };

let getBetaTradeEventDecoderCache: FixedSizeDecoder<BetaTradeEvent> | undefined;

export function getBetaTradeEventDecoder(): FixedSizeDecoder<BetaTradeEvent> {
    return (getBetaTradeEventDecoderCache ??= getStructDecoder([
        ['poolState', getAddressDecoder()],
        ['amountIn', getU64Decoder()],
    ]));
}

/**
 * Checks whether the event data matches the framing and discriminator bytes of a
 * {@link BetaTradeEvent}, without decoding. Never throws.
 *
 * Returns `false` unless `event.programAddress` is BETA_PROGRAM_ADDRESS.
 * Raw bytes carry no emitter and SKIP that check.
 *
 * @see parseBetaTradeEvent to decode the matching data
 */
export function isBetaTradeEvent(
    event: { data: ReadonlyUint8Array; programAddress: Address } | ReadonlyUint8Array,
): boolean {
    if ('data' in event && event.programAddress !== BETA_PROGRAM_ADDRESS) return false;
    const data = 'data' in event ? event.data : event;
    return (
        containsBytes(data, ANCHOR_EVENT_CPI_DISCRIMINATOR, 0) && containsBytes(data, BETA_TRADE_EVENT_DISCRIMINATOR, 8)
    );
}

/**
 * Parses raw event data as a {@link BetaTradeEvent}. Returns `null` on framing or discriminator
 * mismatch; throws if the event matches but its body fails to decode.
 *
 * Returns `null` unless `event.programAddress` is BETA_PROGRAM_ADDRESS.
 * Raw bytes carry no emitter and SKIP that check.
 *
 * @see isBetaTradeEvent to check without decoding
 * @see identifyBetaEvent to identify any program event
 * @see parseBetaEvent
 */
export function parseBetaTradeEvent(
    event: { data: ReadonlyUint8Array; programAddress: Address } | ReadonlyUint8Array,
): BetaTradeEvent | null {
    const checkedEvent = 'data' in event ? { data: event.data, programAddress: event.programAddress } : event;
    if (!isBetaTradeEvent(checkedEvent)) {
        return null;
    }
    const data = 'data' in checkedEvent ? checkedEvent.data : checkedEvent;
    return getBetaTradeEventDecoder().decode(
        data,
        ANCHOR_EVENT_CPI_DISCRIMINATOR.length + BETA_TRADE_EVENT_DISCRIMINATOR.length,
    );
}
