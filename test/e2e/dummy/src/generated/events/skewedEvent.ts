/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    containsBytes,
    fixEncoderSize,
    getBytesEncoder,
    getStructDecoder,
    getU64Decoder,
    type Address,
    type FixedSizeDecoder,
    type ReadonlyUint8Array,
} from '@solana/kit';
import { ALPHA_PROGRAM_ADDRESS } from '../programs/index.js';

export const SKEWED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([17, 17, 17, 17, 17, 17, 17, 17]);

export function getSkewedEventDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(SKEWED_EVENT_DISCRIMINATOR);
}

export type SkewedEvent = { amount: bigint };

let getSkewedEventDecoderCache: FixedSizeDecoder<SkewedEvent> | undefined;

export function getSkewedEventDecoder(): FixedSizeDecoder<SkewedEvent> {
    return (getSkewedEventDecoderCache ??= getStructDecoder([['amount', getU64Decoder()]]));
}

/**
 * Checks whether the event data matches the discriminator bytes of a
 * {@link SkewedEvent}, without decoding. Never throws.
 *
 * Returns `false` unless `event.programAddress` is ALPHA_PROGRAM_ADDRESS.
 * Raw bytes carry no emitter and SKIP that check.
 *
 * @see parseSkewedEvent to decode the matching data
 */
export function isSkewedEvent(
    event: { data: ReadonlyUint8Array; programAddress: Address } | ReadonlyUint8Array,
): boolean {
    if ('data' in event && event.programAddress !== ALPHA_PROGRAM_ADDRESS) return false;
    const data = 'data' in event ? event.data : event;
    return containsBytes(data, SKEWED_EVENT_DISCRIMINATOR, 0) && data.length >= 16;
}

/**
 * Parses raw event data as a {@link SkewedEvent}. Returns `null` on discriminator
 * mismatch; throws if the event matches but its body fails to decode.
 *
 * Returns `null` unless `event.programAddress` is ALPHA_PROGRAM_ADDRESS.
 * Raw bytes carry no emitter and SKIP that check.
 *
 * @see isSkewedEvent to check without decoding
 * @see identifyAlphaEvent to identify any program event
 * @see parseAlphaEvent
 */
export function parseSkewedEvent(
    event: { data: ReadonlyUint8Array; programAddress: Address } | ReadonlyUint8Array,
): SkewedEvent | null {
    const checkedEvent = 'data' in event ? { data: event.data, programAddress: event.programAddress } : event;
    if (!isSkewedEvent(checkedEvent)) {
        return null;
    }
    const data = 'data' in checkedEvent ? checkedEvent.data : checkedEvent;
    return getSkewedEventDecoder().decode(data, 16);
}
