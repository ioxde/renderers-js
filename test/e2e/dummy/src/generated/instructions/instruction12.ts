/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    assertIsInstructionForProgram,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type Instruction,
    type InstructionWithAccounts,
    type ReadonlyAccount,
    type TransactionSigner,
    type WritableSignerAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/kit/program-client-core';
import { DUMMY_PROGRAM_ADDRESS } from '../programs/index.js';

export type Instruction12Instruction<
    TProgram extends string = typeof DUMMY_PROGRAM_ADDRESS,
    TAccountPayer extends string | AccountMeta<string> = string,
    TAccountPinnedOptional extends string | AccountMeta<string> | undefined = undefined,
    TAccountDerivedOptional extends string | AccountMeta<string> | undefined = undefined,
    TAccountProgramOptional extends string | AccountMeta<string> | undefined = undefined,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithAccounts<
        [
            TAccountPayer extends string
                ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
                : TAccountPayer,
            ...(TAccountPinnedOptional extends undefined
                ? []
                : [
                      TAccountPinnedOptional extends string
                          ? ReadonlyAccount<TAccountPinnedOptional>
                          : TAccountPinnedOptional,
                  ]),
            ...(TAccountDerivedOptional extends undefined
                ? []
                : [
                      TAccountDerivedOptional extends string
                          ? ReadonlyAccount<TAccountDerivedOptional>
                          : TAccountDerivedOptional,
                  ]),
            ...(TAccountProgramOptional extends undefined
                ? []
                : [
                      TAccountProgramOptional extends string
                          ? ReadonlyAccount<TAccountProgramOptional>
                          : TAccountProgramOptional,
                  ]),
            ...TRemainingAccounts,
        ]
    >;

export type Instruction12Input<
    TAccountPayer extends string = string,
    TAccountPinnedOptional extends string = string,
    TAccountDerivedOptional extends string = string,
    TAccountProgramOptional extends string = string,
> = {
    payer: TransactionSigner<TAccountPayer>;
    pinnedOptional?: Address<TAccountPinnedOptional>;
    derivedOptional?: Address<TAccountDerivedOptional>;
    programOptional?: Address<TAccountProgramOptional>;
};

export function getInstruction12Instruction<
    TAccountPayer extends string,
    TAccountPinnedOptional extends string,
    TAccountDerivedOptional extends string,
    TAccountProgramOptional extends string,
>(
    input: Instruction12Input<TAccountPayer, TAccountPinnedOptional, TAccountDerivedOptional, TAccountProgramOptional>,
): Instruction12Instruction<
    typeof DUMMY_PROGRAM_ADDRESS,
    TAccountPayer,
    TAccountPinnedOptional,
    TAccountDerivedOptional,
    TAccountProgramOptional
> {
    // Program address.
    const programAddress = DUMMY_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        payer: { value: input.payer ?? null, isWritable: true },
        pinnedOptional: { value: input.pinnedOptional ?? null, isWritable: false },
        derivedOptional: { value: input.derivedOptional ?? null, isWritable: false },
        programOptional: { value: input.programOptional ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    const getAccountMeta = getAccountMetaFactory(programAddress, 'omitted');
    return Object.freeze({
        accounts: [
            getAccountMeta('payer', accounts.payer),
            getAccountMeta('pinnedOptional', accounts.pinnedOptional),
            getAccountMeta('derivedOptional', accounts.derivedOptional),
            getAccountMeta('programOptional', accounts.programOptional),
        ].filter(<T>(x: T | undefined): x is T => x !== undefined),
        programAddress,
    } as Instruction12Instruction<
        typeof DUMMY_PROGRAM_ADDRESS,
        TAccountPayer,
        TAccountPinnedOptional,
        TAccountDerivedOptional,
        TAccountProgramOptional
    >);
}

export type ParsedInstruction12Instruction<
    TProgram extends string = typeof DUMMY_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        payer: TAccountMetas[0];
        pinnedOptional?: TAccountMetas[1] | undefined;
        derivedOptional?: TAccountMetas[2] | undefined;
        programOptional?: TAccountMetas[3] | undefined;
    };
};

export function parseInstruction12Instruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas>,
): ParsedInstruction12Instruction<TProgram, TAccountMetas> {
    assertIsInstructionForProgram(instruction, DUMMY_PROGRAM_ADDRESS);

    if (instruction.accounts.length < 1) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 1,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    let optionalAccountsRemaining = instruction.accounts.length - 1;
    const getNextOptionalAccount = () => {
        if (optionalAccountsRemaining === 0) return undefined;
        optionalAccountsRemaining -= 1;
        return getNextAccount();
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            payer: getNextAccount(),
            pinnedOptional: getNextOptionalAccount(),
            derivedOptional: getNextOptionalAccount(),
            programOptional: getNextOptionalAccount(),
        },
    };
}
